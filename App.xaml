<Application x:Class="Automatic_Replay_Buffer.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Automatic_Replay_Buffer"
             xmlns:viewmodel="clr-namespace:Automatic_Replay_Buffer.ViewModel"
             xmlns:view="clr-namespace:Automatic_Replay_Buffer.View"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>

            <DataTemplate DataType="{x:Type viewmodel:HomeViewModel}">
                <view:HomeView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewmodel:SettingsViewModel}">
                <view:SettingsView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewmodel:GameListViewModel}">
                <view:GameListView />
            </DataTemplate>

            <DataTemplate DataType="{x:Type viewmodel:FilterListViewModel}">
                <view:FilterListView />
            </DataTemplate>

        </ResourceDictionary>
    </Application.Resources>
</Application>
