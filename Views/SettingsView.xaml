<Window x:Class="Automatic_Replay_Buffer.Views.SettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Automatic_Replay_Buffer.Views"
        xmlns:viewmodel="clr-namespace:Automatic_Replay_Buffer.ViewModel"
        d:DataContext="{d:DesignInstance Type=viewmodel:MainViewModel}"
        mc:Ignorable="d"
        Title="SettingsView"
        Height="297"
        Width="300"
        MinWidth="300"
        MinHeight="297">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical"
                    Margin="20">
            
            <TextBlock HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="24"
                       Text="Twitch" 
                       TextDecorations="Underline"/>

            <TextBox Width="250"
                     Height="30"
                     FontSize="16"
                     Margin="5"
                     Text="{Binding StorageService.Client.ID}" />

            <TextBox Width="250"
                     Height="30"
                     FontSize="16"
                     Margin="5"
                     Text="{Binding StorageService.Client.Secret}" />

            <TextBlock HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       FontSize="24"
                       Text="Websocket"
                       TextDecorations="Underline" 
                       Margin="0,20,0,0"/>

            <TextBox Width="250"
                     Height="30"
                     FontSize="16"
                     Margin="5"
                     Text="{Binding StorageService.OBS.Address}" />

            <TextBox Width="250"
                     Height="30"
                     FontSize="16"
                     Margin="5"
                     Text="{Binding StorageService.OBS.Password}" />
        </StackPanel>
    </Grid>
</Window>
